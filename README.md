# miniature-waffle


if u using humble:

do this from the root repo

source /opt/ros/humble/setup.bash

source /usr/share/gazebo/setup.sh

then

colcon build 

source install/setup.bash

ros2 launch sensor_fusion_desc env.launch.py

